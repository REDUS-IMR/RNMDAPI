# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R

matrix:
  include:
    - os: linux
      r: release
    - os: osx
      r: release

sudo: false
cache: packages
